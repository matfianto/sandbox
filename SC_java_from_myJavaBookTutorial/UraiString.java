public class UraiString {
    public static void main(String[] args) {
        String st = "ABCDE";
        
        char[] daftarKarakter = st.toCharArray();

        // Tampilkan isi array
        for (int j = 0; j < daftarKarakter.length; j++)
             System.out.println(daftarKarakter[j]);           
    }
}
